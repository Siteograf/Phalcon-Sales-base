$(document).ready(function () {

    // Подключаем Select2 для удобного выбора (селкт боксы)
    $("select").select2(
        {
            language: "ru",
            // theme: "bootstrap"
        }
    );






});
